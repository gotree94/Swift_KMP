# 0.0 Mac 개발 환경 설정

> 학습을 시작하기 전에 Mac에 필요한 모든 도구를 설치하고 사용 가능한 상태로 만든다.

---

## 학습 목표

- Xcode, Android Studio, JDK, Git을 설치할 수 있다.
- iOS 시뮬레이터와 Android 에뮬레이터를 실행할 수 있다.
- 터미널 사용법(기본 명령어)에 익숙해진다.

---

## 1. 사전 확인

### 1-1. Mac 사양 확인

터미널에서 다음 명령어로 Mac 정보를 확인합니다.

```bash
sw_vers        # macOS 버전 (macOS 14 Sonoma 이상 권장)
sysctl -n hw.availcpu   # cpu 아키텍처 (arm64 = Apple Silicon)
uname -m       # arm64 또는 x86_64
```

- Apple Silicon(arm64)이라면 추후 모든 도구가 자동으로 arm64 버전을 받습니다.
- 디스크 여유 공간 확인: `df -h /`

### 1-2. Apple ID 준비

- 앱스토어에서 Xcode를 받거나 Apple 개발자 계정(무료)을 준비합니다.
- 실제 기기에서 테스트하려면 Apple ID가 필요합니다 (무료 계정 가능).

---

## 2. Xcode 설치

Xcode는 iOS 개발의 필수 IDE입니다.

### 방법 A: App Store 설치 (권장)

1. App Store에서 **Xcode** 검색
2. 설치 (크기 약 12GB+, 시간 소요)
3. 첫 실행 시 추가 컴포넌트(시뮬레이터 등)가 자동 설치됨

### 방법 B: 명령줄 도구만 설치

Swift 문법만 CLI로 연습할 때는 라이트하게 설치할 수 있습니다.

```bash
xcode-select --install
```

### 설치 확인

```bash
xcode-select -p          # /Applications/Xcode.app/Contents/Developer
swift --version          # Swift 버전 확인
```

> 이후 실습 전에 라이선스 동의가 필요할 수 있습니다: `sudo xcodebuild -license accept`

---

## 3. Command Line Tools 터미널 기본기

이 커리큘럼은 터미널을 자주 사용합니다. 익숙하지 않다면 아래를 익히고 시작하세요.

```bash
pwd                # 현재 위치
ls -la             # 파일 목록
cd ~/Desktop       # 폴더 이동
mkdir myapp        # 폴더 생성
touch hello.swift  # 빈 파일 생성
open hello.swift   # 기본 앱으로 열기 (Xcode)
cat hello.swift    # 파일 내용 출력
```

**실습**: `~/Desktop/workspace` 폴더를 만들고, `hello.swift` 파일을 생성한 뒤 목록을 확인해보세요.

---

## 4. Homebrew 설치 (편의 도구)

Homebrew는 Mac용 패키지 관리자로, 이후 Git/기타 도구 설치를 쉽게 해줍니다.

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

설치 확인:

```bash
brew --version
```

---

## 5. Git 설치 및 설정

```bash
brew install git        # Homebrew로 설치 (신형 macOS에는 기본 포함)
git --version
```

Git 사용자 정보 등록 (커밋할 때 필요):

```bash
git config --global user.name "내이름"
git config --global user.email "me@example.com"
git config --global init.defaultBranch main
```

GitHub 계정이 없다면 가입하고, 이후 실습 프로젝트를 올릴 준비를 합니다.
(터미널에서 `ssh-keygen` 후 GitHub에 공개 키 등록하는 작업도 추천)

---

## 6. JDK 설치

Kotlin/Android 개발에는 JDK(Java)가 필요합니다. 최신 Android Studio는 JDK 17+ 를 사용합니다.

```bash
brew install --cask temurin@17   # Eclipse Temurin JDK 17
java -version                    # 확인
```

> Android Studio 안에 내장 JDK가 있어서 없어도 동작하지만, CLI 빌드/최신 도구 호환성을 위해 직접 설치를 권장합니다.

---

## 7. Android Studio 설치

Kotlin/KMP의 주 IDE입니다.

1. https://developer.android.com/studio 에서 Android Studio 다운로드
2. `.dmg` 파일 실행 후 `/Applications`로 드래그 설치
3. 첫 실행에서 설정 마법사 진행 (다크 모드 설정, 설치 유형 선택)
4. **SDK Manager**에서 Android SDK 설치 확인

설치 후 터미널에서 SDK 경로 확인:

```bash
echo $ANDROID_HOME     # 권장: ~/Library/Android/sdk
# 없으면 아래 내용을 ~/.zshrc 에 추가
# export ANDROID_HOME="$HOME/Library/Android/sdk"
# export PATH="$PATH:$ANDROID_HOME/platform-tools"
```

---

## 8. 에뮬레이터 / 시뮬레이터 준비

### iOS 시뮬레이터 (Xcode)

1. Xcode 실행 → `Xcode > Settings > Platforms` 
2. 원하는 iOS 버전과 디바이스(예: iPhone 15)를 추가
3. Xcode 열기 메뉴에서 `Devices...` 로 목록 확인

### Android 에뮬레이터 (Android Studio)

1. Android Studio 실행 → `More Actions > Virtual Device Manager`
2. `Create Device` → 픽셀 기기 선택 → 원하는 안드로이드 버전 이미지 다운로드
3. 에뮬레이터 시작 테스트

---

## 9. 컨벤션: 이 커리큘럼에서 쓰는 명령어

- 터미널 명령어는 `bash` 코드 블록에 표기합니다.
- Swift 파일 실행: `swift 파일명.swift`
- Kotlin 파일 실행: `kotlinc 파일명.kt -include-runtime -d out.jar && java -jar out.jar`
- 주석은 `//` 또는 `#` 입니다.

---

## 🛠 실습: 개발 환경 체크리스트

각 항목을 터미널 명령으로 하나씩 확인하고 모두 통과할 때까지 진행하세요.

```bash
# 1. Xcode/Swift 설치 확인
swift --version

# 2. Git 설치 확인 및 환경 변수 출력
git --version
git config user.name
git config user.email

# 3. JDK 확인
java -version

# 4. SDK 환경 변수 확인
echo $ANDROID_HOME

# 5. 시뮬레이터 목록
xcrun simctl list devices | head -20

# 6. 에뮬레이터 목록 (Android Studio에서 확인)
# emulator -list-avds
```

## 실습 힌트

- `swift --version`이 나오지 않으면 `xcode-select --install` 후 macOS를 재시작하세요.
- `$ANDROID_HOME`이 비어 있어도 Android Studio가 설치되어 있으면 에뮬레이터는 동작합니다.

## 마무리

이제 Swift 문법부터 Kotlin, KMP까지 학습할 수 있는 완전한 Mac 환경이 갖춰졌습니다.
다음 챕터 `01_Swift/01_첫_Swift와_기초_문법.md` 로 진행하세요.